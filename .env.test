# --- Configuración del Entorno de la Aplicación ---
# Define en qué modo está corriendo la aplicación. Opciones: development | testing | production
ENV=tetsting

# --- Configuración de la Base de Datos ---
# Estas variables son leídas por el servicio 'bff_mobil_postgres' en docker-compose.yml
# y también por tu aplicación FastAPI para construir la URL de conexión.
# NOTA: POSTGRES_HOST se define en docker-compose.yml para la comunicación entre contenedores.
POSTGRES_HOST=localhost
POSTGRES_USER=user_emergqr
POSTGRES_PASSWORD=EmergQR2002
POSTGRES_DB=emergqr_test
POSTGRES_PORT=5432

# --- Configuración de JWT (JSON Web Tokens) ---
# ¡IMPORTANTE! Usa un secreto largo, aleatorio y seguro en un entorno real.
JWT_SECRET=3dd3be27fa3d5a83ecd749956f6a336d1b34c76a33213e283dfe8e53e24caf4b
JWT_ALGORITHM=HS256
# Define el tiempo de expiración del JWT en minutos. Por defecto, 1440 minutos (24 horas).
# Puedes cambiarlo a un valor más corto o más largo según tus necesidades.
JWT_EXPIRE_MINUTES=1440
# --- Configuración de Almacenamiento y Aplicación ---
# La ruta de almacenamiento es manejada por el volumen de Docker, por lo que esta variable es opcional.
STORAGE_PATH=./storage

MAX_FILE_SIZE_MB=10

#variable de uso global para el proyecto
# GLOBAL_VARIABLE=some_value
# --- Variables de Lógica de Negocio ---
# Estas son leídas por tu aplicación desde config.py
REGISTER_MIN_QUERY=0
REGISTER_MAX_QUERY=100

# Las siguientes variables son específicas de tu lógica pero no están actualmente
# definidas en la clase Settings de 'app/core/config.py'. La aplicación las ignorará
# a menos que las añadas a dicha clase.
REGISTER_DEFAULT_QUERY=10
REGISTER_MAX_QUERY_LIMIT=1000
# --- Políticas de Contraseña ---
MIN_LENGTH_PASSWORD=8
MAX_LENGTH_PASSWORD=64
PASSWORD_COMPLEXITY=strong
PASSWORD_EXPIRATION_DAYS=90
PASSWORD_HISTORY_LIMIT=5

# Optional: Uncomment and set the following if you want to use Redis
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password

# Optional: Uncomment and set the following if you want to use RabbitMQ
# RABBITMQ_HOST=localhost
# RABBITMQ_PORT=5672
# RABBITMQ_USER=guest
# RABBITMQ_PASSWORD=guest

# Optional: Uncomment and set the following if you want to use Elasticsearch
# ELASTICSEARCH_HOST=localhost
# ELASTICSEARCH_PORT=9200
# ELASTICSEARCH_USER=elastic
# ELASTICSEARCH_PASSWORD=your_elasticsearch_password

# Optional: Uncomment and set the following if you want to use MongoDB
# MONGODB_URI=mongodb://localhost:27017/emergqr_dev
# Optional: Uncomment and set the following if you want to use MinIO
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=your_minio_access_key
# MINIO_SECRET_KEY=your_minio_secret_key
# MINIO_BUCKET=emergqr-bucket
# MINIO_USE_SSL=false
# MINIO_REGION=us-east-1
# MINIO_USE_PATH_STYLE=true

# variables for the email service# EMAIL_SERVICE=your_email_service
# EMAIL_HOST=smtp.your_email_service.com
# EMAIL_PORT=587
# EMAIL_USER=your_email_user
# EMAIL_PASSWORD=your_email_password
# EMAIL_FROM=your_email_from_address
# EMAIL_FROM_NAME=Your Name or Company
# EMAIL_USE_TLS=true
# EMAIL_USE_SSL=false
# Optional: Uncomment and set the following if you want to use a third-party API
# THIRD_PARTY_API_URL=https://api.thirdparty.com
# THIRD_PARTY_API_KEY=your_third_party_api_key
# Optional: Uncomment and set the following if you want to use a caching layer
GOOGLE_API_KEY="AIzaSyDMJNoP1xPZCbPVbRas6U9ArZ62HhnUtM8"
